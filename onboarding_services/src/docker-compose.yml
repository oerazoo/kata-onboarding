services:
  postgres:
    image: 'postgres:latest'
    restart: unless-stopped
    environment:
      - 'POSTGRES_DB=kata_bank_db'
      - 'POSTGRES_PASSWORD=okn'
      - 'POSTGRES_USER=okn'
    ports:
      - '5432:5432'
    volumes:
      - pgdata:/data/postgres


volumes:
  pgdata: